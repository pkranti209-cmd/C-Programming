//2. Write a C program to find the area of a circle.
#include <stdio.h>

int main()
{
    float radius = 7;
    float area = 3.14 * radius * radius;

    printf("Area = %.2f", area);

    return 0;
}